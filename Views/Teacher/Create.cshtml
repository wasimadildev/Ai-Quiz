@model QuizFormViewModel
@{
    ViewData["Title"] = "Create Quiz";
}
<div class="dashboard-header">
    <h1><i class="fas fa-plus-circle me-2 text-gradient"></i>Create Quiz</h1>
</div>
<div class="card quiz-form-card">
    <div class="card-body">
        <form asp-action="Create" method="post">
            <partial name="_QuizForm" model="Model" />
            <div class="d-flex gap-2 mt-4">
                <button type="submit" class="btn btn-primary btn-lg"><i class="fas fa-save me-2"></i>Save Quiz</button>
                <a class="btn btn-outline-secondary btn-lg" asp-action="Quizzes"><i class="fas fa-times me-1"></i>Cancel</a>
            </div>
        </form>
    </div>
</div>
@section Scripts { <partial name="_ValidationScriptsPartial" /> }
